"""PHANTOM-ECHO REVEAL — Real-time streaming layer (Phase 2).

Wraps the v21 batch pipeline (src/main_v2.py modules) into an incremental,
frame-by-frame engine that emits JSON events consumable over WebSocket.
"""
